spring:
  application:
    name: placas_db
  datasource:
    url: ${DB_URL:jdbc:postgresql://127.0.0.1:5432/placas_db}
    username: ${DB_USER:postgres}
    password: ${DB_PASSWORD:123}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
cloudinary:
  url: ${CLOUDINARY_URL:cloudinary://154927418879722:**********@do7yferaz}